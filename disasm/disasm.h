#pragma once
#include <windows.h>
#include <iostream>

namespace ASM {
    class Disasm{
        private:

        public:
            Disasm() = default;
            ~Disasm() = default;
    };
};